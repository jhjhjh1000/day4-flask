services:
  - type: web
    name: day4-flask
    runtime: python
    buildCommand: bash build.sh
    startCommand: python seed.py && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.10"
